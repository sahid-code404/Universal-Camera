# processing-api

Future processing contracts only.

No custom HDR, Night, denoise, super-resolution, or semantic-processing implementation belongs in Phase 0/1.

User-visible processing controls must not be shipped before the underlying feature exists.
